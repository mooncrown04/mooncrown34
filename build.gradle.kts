// build.gradle.kts (ana dizin)
buildscript {
    repositories {
        google()
        mavenCentral()
        maven("https://jitpack.io")
        maven("https://repo.recloudstream.com/releases")
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven("https://jitpack.io")
        maven("https://repo.recloudstream.com/releases")
    }
}
