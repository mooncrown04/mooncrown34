plugins {
    id("cloudstream-plugin") version "1.0.0" // ya da mevcutsa güncel sürüm
}

cloudstream {
    language.set("tr")
    description.set("M3U yayın sağlayıcısı")
    authors.set(listOf("mooncrown04"))
}
